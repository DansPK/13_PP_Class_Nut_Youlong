# Environment Configuration for RAG Chat with Documents

# --- Models ---
# Embedding model (must be available in Ollama or API)
EMBED_MODEL=nomic-embed-text

# Generation model (must be available in Ollama or API)
GEN_MODEL=llama3:8b

# --- Storage ---
# Directory containing source documents
DATA_DIR=data

# Directory for vector store persistence (ChromaDB)
CHROMA_DB_DIR=chroma_db

# Vector store collection name
COLLECTION_NAME=documents

# --- Chunking ---
# Size of text chunks in characters
CHUNK_SIZE=800

# Overlap between consecutive chunks in characters
CHUNK_OVERLAP=120

# --- Retrieval ---
# Number of chunks to retrieve per question
TOP_K=4

# --- Vector Store Configuration ---
# Options: chromadb, pgvector, qdrant
VECTOR_STORE_TYPE=chromadb

# PostgreSQL connection string (for pgvector)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pgvector_db

# Qdrant configuration
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION_NAME=documents

# OpenAI-compatible endpoint (e.g. local LLM server, LM Studio, vLLM, Azure OpenAI, etc.)
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_COMPATIBLE_API_KEY=your_api_key_here



